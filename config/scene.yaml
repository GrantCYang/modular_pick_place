# ─────────────────────────────────────────────
#  场景配置文件
# ─────────────────────────────────────────────

spawn:
  x_range: [-0.30, -0.15]
  y_range: [-0.15, 0.15]
  min_distance: 0.08

target_area:
  center: [0.02, 0.0]
  size:   [0.18, 0.16]
  color:  [1.0, 0.85, 0.0, 0.4]

objects:
  - id: box_0
    category: box
    half_size: [0.025, 0.025, 0.025]
    color: [0.8, 0.2, 0.2, 1.0]

  - id: obj_tuna
    category: ycb
    model_id: "007_tuna_fish_can"  

  - id: obj_orange
    category: ycb
    model_id: "017_orange"    # ← YCB mustard bottle
    color: ~                           # ycb 使用自带贴图，color 忽略